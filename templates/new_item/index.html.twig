{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Création item{% endblock %}
 
{% block body %}
    {{ form(form) }}
    {{ form(formRarity) }}
    {{ form(formWeaponAS) }}
    {{ form(formType) }}
{% endblock %}
